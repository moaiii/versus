language: node_js
node_js:
  - node

scripts:
  - npm install
  - npm run test
  - npm run build

deploy:
  provider: s3
  access_key_id: $access_key_id
  secret_access_key: $secret_access_key
  bucket: "versus-mvp"

branches:
  only:
    - master
    - mvp
    - chris/mvp-1-s3-bucket