import reducer from "./RootSelectors.reducer";
import * as actions from "./RootSelectors.action";
import store from '../../../db/store';

describe('RootSelectors', () => {
  it('sample test', async () => {
    expect(true).toBe(true);
  })
});
