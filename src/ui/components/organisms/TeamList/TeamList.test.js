import reducer from "./TeamList.reducer";
import * as actions from "./TeamList.action";
import store from '../../../db/store';

describe('TeamList', () => {
  it('sample test', async () => {
    expect(true).toBe(true);
  })
});
