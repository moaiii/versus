import reducer from "./GamesList.reducer";
import * as actions from "./GamesList.action";
import store from '../../../db/store';

describe('GamesList', () => {
  it('sample test', async () => {
    expect(true).toBe(true);
  })
});
