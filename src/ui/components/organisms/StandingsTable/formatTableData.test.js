import formatTableData from './formatTableData';

describe('formatTableData', () => {
  it('should return an object with the required keys', () => {
    const requiredKeys = ['Position', 'Club', 'Played', 'Won', 'Drawn',
      'Lost', 'GF', 'GA', 'GD', 'Points'];

    expect(true).toBe(true);
  })
})
