// @flow
import * as React from "react";

type Props = {};
type State = {};

const Team = ( {props} ): React.Element<'div'> => {
  const {} = props;

  return(
    <div className={`Team`}>
    
    </div>
  )
}

export default Team;
