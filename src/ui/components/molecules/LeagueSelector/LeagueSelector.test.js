import reducer from "./LeagueSelector.reducer";
import * as actions from "./LeagueSelector.action";
import store from '../../../db/store';

describe('LeagueSelector', () => {
  it('sample test', async () => {
    expect(true).toBe(true);
  })
});
