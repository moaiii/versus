import reducer from "./CountrySelector.reducer";
import * as actions from "./CountrySelector.action";
import store from '../../../db/store';

describe('CountrySelector', () => {
  it('sample test', async () => {
    expect(true).toBe(true);
  })
});
