import reducer from "./DualSlider.reducer";
import * as actions from "./DualSlider.action";
import store from '../../../db/store';

describe('DualSlider', () => {
  it('sample test', async () => {
    expect(true).toBe(true);
  })
});
